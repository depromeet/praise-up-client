import { Preview } from "./preview";

import { DefaultLayout } from "@/components/layout/default";

export const Archive = () => {
  return (
    <DefaultLayout>
      <Preview />
    </DefaultLayout>
  );
};
